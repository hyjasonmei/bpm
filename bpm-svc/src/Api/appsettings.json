{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Database": {
    "_doc": "Provider = sqlite | postgres. Tests use in-memory SQLite directly; the running app uses this. Per-environment, override ConnectionStrings:Default + Database:Provider via env (ConnectionStrings__Default / Database__Provider).",
    "Provider": "postgres"
  },
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=flowcook;Username=flowcook;Password=flowcook"
  },
  "Cors": {
    "BpmUiOrigin": "http://localhost:5173,http://localhost:5174,http://localhost:5175"
  },
  "Files": {
    "MaxBytes": 10485760,
    "RootPath": "files"
  }
}
