# Fixed Asset Purchase 固定資產採購 (FAP v1)

This bundle packages the **Fixed Asset Purchase 固定資產採購** flow for portable installation.
It contains 6 flow nodes, including 2 user task(s) and 1 approval step(s).

See `spec.md` for the human-readable overview, `walkthrough.md` for the happy-path narration, and `manifest.json` for the canonical file list with SHA-256 checksums.
