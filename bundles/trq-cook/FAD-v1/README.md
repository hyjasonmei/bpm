# Fixed Asset Disposal 固定資產處分 (FAD v1)

This bundle packages the **Fixed Asset Disposal 固定資產處分** flow for portable installation.
It contains 5 flow nodes, including 2 user task(s) and 1 approval step(s).

See `spec.md` for the human-readable overview, `walkthrough.md` for the happy-path narration, and `manifest.json` for the canonical file list with SHA-256 checksums.
