---
import BaseLayout from '../layouts/BaseLayout.astro'
import Screenshot from '../components/Screenshot.astro'
---
<BaseLayout title="一道菜的旅程 — flowcook">
  <section class="bg-header py-16 text-white sm:py-24">
    <div class="container-narrow max-w-3xl">
      <p class="font-mono text-xs uppercase tracking-[0.2em] text-accent">How it works</p>
      <h1 class="mt-3 text-5xl font-semibold leading-tight text-white sm:text-6xl">
        一道菜的旅程
      </h1>
      <p class="mt-6 text-lg leading-relaxed text-white/85">
        從你說「我們想做一個請假流程」，到員工真的在公司用它請假 ——
        flowcook 把這條路徑切成 11 個你看得懂、AI 幫你跑的步驟。
      </p>
    </div>
  </section>

  <section class="bg-page py-16 sm:py-20">
    <div class="container-narrow max-w-3xl space-y-8">
      <article class="rounded-lg border border-rule bg-card p-8 shadow-sm">
        <p class="font-mono text-xs uppercase tracking-[0.2em] text-primary">Step 1 · SOURCE</p>
        <h2 class="mt-2 text-3xl font-semibold">從一張圖、一份 PDF、一句話開始</h2>
        <p class="mt-3 text-base text-ink/85">
          上傳老闆畫的流程圖、HR 給的 PDF SOP、或者你自己用一段話描述。
          AI 把它變成 BPMN 骨架 —— 起點、終點、節點、線通通畫好。
        </p>
        <div class="mt-6">
          <Screenshot
            src="/screenshots/wizard-hero.png"
            alt="flowcook admin AI Kitchen wizard — 第 1 步，對話式描述流程 + BPMN canvas"
            label="flowcook · admin"
            caption="AI Kitchen — 邊聊邊產生 BPMN"
          />
        </div>
      </article>

      <article class="rounded-lg border border-rule bg-card p-8 shadow-sm">
        <p class="font-mono text-xs uppercase tracking-[0.2em] text-primary">Step 2 · FORMS</p>
        <h2 class="mt-2 text-3xl font-semibold">每個任務的表單 —— 細到欄位寬度</h2>
        <p class="mt-3 text-base text-ink/85">
          區塊、多欄列、條件顯示、提示文字、發票多筆 —— 真實 BPM form 該有的版面元素都在。
          chef 之後寫 code 時，照這個 layout 渲染 —— 下面就是 cook 出來的真實表單。
        </p>
        <div class="mt-6">
          <Screenshot
            src="/screenshots/flow-form.png"
            alt="flowcook 員工端流程表單 — 步驟條、區塊、明細多筆"
            label="flowcook · 員工端"
            caption="spec 定義的版面，chef 渲染成真實表單"
          />
        </div>
      </article>

      <article class="rounded-lg border border-rule bg-card p-8 shadow-sm">
        <p class="font-mono text-xs uppercase tracking-[0.2em] text-primary">Step 3-11</p>
        <h2 class="mt-2 text-3xl font-semibold">存取、決策、審核、通知、SLA、整合、變數、翻譯、備註</h2>
        <p class="mt-3 text-base text-ink/85">
          每一步都是 AI 邊問邊填，最後產出一份完整 spec。
          這份 spec 是 chef 進廚房的食譜 —— 也是給 IT 部門 review 的合約。
        </p>
        <div class="mt-6">
          <Screenshot
            src="/screenshots/ai-kitchen.png"
            alt="flowcook admin AI Kitchen — spec 煮成的流程全部上線"
            label="flowcook · admin"
            caption="spec 煮成流程 —— 一張表看所有上線狀態"
          />
        </div>
      </article>

      <article class="rounded-lg border border-accent/30 bg-accent/5 p-8">
        <p class="font-mono text-xs uppercase tracking-[0.2em] text-accent">Chef 進廚房</p>
        <h2 class="mt-2 text-3xl font-semibold">spec 交出去 —— AI 寫真實代碼</h2>
        <p class="mt-3 text-base text-ink/85">
          chef 讀 spec、讀現有 reference code、寫出新的 React + .NET。
          所有變更落在獨立 git branch；你 review 完才 merge。煮好上線後，員工就像下面這樣每天用它。
        </p>
        <div class="mt-6">
          <Screenshot
            src="/screenshots/case-detail.png"
            alt="flowcook 員工端案件詳情 — 簽核時序軸與核准 / 退件"
            label="flowcook · 員工端"
            caption="上桌 —— 員工每天在用的真實簽核畫面"
          />
        </div>
      </article>
    </div>
  </section>
</BaseLayout>
