# 採購申請流程 (PURCHASE_REQUEST v1)

This bundle packages the **採購申請流程** flow for portable installation.
It contains 8 flow nodes, including 1 user task(s) and 3 approval step(s).

See `spec.md` for the human-readable overview, `walkthrough.md` for the happy-path narration, and `manifest.json` for the canonical file list with SHA-256 checksums.
