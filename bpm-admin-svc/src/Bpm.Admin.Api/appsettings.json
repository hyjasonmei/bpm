{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Database": {
    "_doc": "Provider = sqlite | postgres. admin-svc + bpm-svc share one physical DB. Override ConnectionStrings:Default + Database:Provider via env per environment.",
    "Provider": "postgres"
  },
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=flowcook;Username=flowcook;Password=flowcook"
  }
}
