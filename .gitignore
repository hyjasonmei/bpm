# Reference materials (not committed — same convention as the ghp project)
refs/

# Wizard hand-off — incoming spec.json drops from POST /api/spec
incoming/
**/incoming/
specs-incoming/

# OS
.DS_Store
Thumbs.db

bpm-admin-ui/node_modules/

# Local launcher output (launch.sh writes BE/FE logs + verify screenshots here)
.launch-logs/

# Claude Code per-project local settings
.claude/settings.local.json

# bpm-www (Astro)
bpm-www/node_modules/
bpm-www/dist/
bpm-www/.astro/

bpm-admin-ui/dist/

# chef-agent real config holds tokens — never commit
chef/agent/chef-agent.json
chef/agent/**/bin/
chef/agent/**/obj/

# runtime notification capture log (dev artifact)
bpm-svc/src/Api/var/
